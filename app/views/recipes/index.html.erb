<!-- <div class="orbit" role="region" aria-label="Favorite Space Pictures" data-orbit>
  <ul class="orbit-container">
    <button class="orbit-previous"><span class="show-for-sr">Previous Slide</span>&#9664;&#xFE0E;</button>
    <button class="orbit-next"><span class="show-for-sr">Next Slide</span>&#9654;&#xFE0E;</button>
    <li class="is-active orbit-slide">
      <%# image_tag('veggies.jpg', class: "orbit-image", alt: "Biscuits") %>
    </li>
    <li class="orbit-slide">
      <%# image_tag('pineapples.jpeg', class: "orbit-image", alt: "Pineapples") %>
    </li>
    <li class="orbit-slide">
      <%# image_tag('peppers.jpeg', class: "orbit-image", alt: "Peppers") %>
    </li>
    <li class="orbit-slide">
      <%# image_tag('pizza.jpg', class: "orbit-image", alt: "Pizza") %>
    </li>
  </ul>
  <nav class="orbit-bullets">
    <button class="is-active" data-slide="0"><span class="show-for-sr">First slide details.</span><span class="show-for-sr">Current Slide</span></button>
    <button data-slide="1"><span class="show-for-sr">Second slide details.</span></button>
    <button data-slide="2"><span class="show-for-sr">Third slide details.</span></button>
    <button data-slide="3"><span class="show-for-sr">Fourth slide details.</span></button>
  </nav> -->
<main class="home">
<article class="search-section">
  <h3 class="help-with">What Can We Help You Find? </h3>
  <div class="the-form">
  <%= form_tag(search_path(page: 0), method: :get) do %>
    <%# label_tag(:search_term, "Search For:") %>
    <%= text_field_tag(:search_term) %>

    <%# @all_health.each do |label| %>
      <%# label_tag(label) %>
      <%# check_box_tag "healthLabels[]", label %>
    <%# end %>

    <!-- Note - turns out you can only search by vegan and vegitarian - the other health labels don't work -->
    <%= submit_tag("SEARCH", class: 'secondary button search-button') %>
  <% end %>
  </div>
</article>
</main>
